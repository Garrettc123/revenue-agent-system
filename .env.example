# Revenue Agent System - Environment Configuration Template
# 
# IMPORTANT: Copy this file to .env and fill in your actual values
# Command: cp .env.example .env
#
# DO NOT commit .env file to version control (already in .gitignore)

# =============================================================================
# PAYMENT CONFIGURATION
# =============================================================================
# PayPal account email for receiving payments
# Example: your-email@example.com
PAYMENT_ACCOUNT=your-paypal-email@example.com

# =============================================================================
# API SECURITY
# =============================================================================
# API authentication token for MLOps and internal services
# Generate a secure random token: openssl rand -hex 32
# Example: abc123def456...
API_TOKEN=your-secure-api-token-here

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL database name
POSTGRES_DB=revenue_agent

# PostgreSQL user
POSTGRES_USER=agent

# PostgreSQL password
# Generate a secure password: openssl rand -base64 32
# IMPORTANT: Use a strong password in production
POSTGRES_PASSWORD=your-secure-postgres-password

# =============================================================================
# GRAFANA MONITORING
# =============================================================================
# Grafana admin password
# IMPORTANT: Change this from default for production
# Generate: openssl rand -base64 24
GF_SECURITY_ADMIN_PASSWORD=your-secure-grafana-password

# Grafana server URL (usually keep as default for local development)
GF_SERVER_ROOT_URL=http://localhost:3000

# =============================================================================
# TRADING CONFIGURATION
# =============================================================================
# Comma-separated list of stock tickers to trade
TICKERS=AAPL,TSLA,MSFT,NVDA,GOOGL,AMZN

# =============================================================================
# REDIS CONFIGURATION
# =============================================================================
# Redis host (use 'redis' for docker-compose, 'localhost' for local dev)
REDIS_HOST=redis

# =============================================================================
# NOTES
# =============================================================================
# - All sensitive values should be kept secret and never committed
# - Use strong, unique passwords for production environments
# - Rotate credentials regularly
# - See DEPLOYMENT.md for detailed setup instructions
